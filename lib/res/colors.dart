import 'package:flutter/material.dart';

///Here we will keep references to colors like a style
class SupportDeskColors {
  static const Color appBackground = Color(0xFFF6F6F6);
  static const Color primaryColor = Color(0xFFD50032);
  static const Color accentColor = Colors.pinkAccent;

  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Colors.grey;

  SupportDeskColors._();
}
